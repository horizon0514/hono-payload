{
  "buildCommand": "bun run build:db && bun run build:cms",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "bun install",
  "env": {
    "DATABASE_URI": "@database_uri",
    "PAYLOAD_SECRET": "@payload_secret"
  }
}
